const React = require('react');

const Colors = (props) => {
  return (
    <h3>Colors {/* name of color here */}</h3>
    // colors array in DB passed in
    // render color squares based on colors in DB
  )
}

export default Colors;